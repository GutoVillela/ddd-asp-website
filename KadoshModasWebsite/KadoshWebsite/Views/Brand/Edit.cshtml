@model BrandViewModel
@{
    ViewData["Title"] = $"Editando a loja {Model.Name}";
}

<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-12">
        <form asp-action="Edit" method="post" class="row">
            <input asp-for="Id" type="hidden" />
            <div class="col-12 mb-3">
                <div class="input-group flex-nowrap">
                    <span class="input-group-text" id="addon-wrapping"><i class="uil uil-ticket icon-size-1-5"></i></span>
                    <div class="form-floating w-100">
                        <input asp-for="Name" class="form-control" placeholder="Nome da marca" />
                        <label asp-for="Name"></label>
                    </div>
                </div>
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="col-md-12">
                <input type="submit" value="Atualizar" class="btn btn-primary" />
                <a class="btn btn-danger" asp-action="Index">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}