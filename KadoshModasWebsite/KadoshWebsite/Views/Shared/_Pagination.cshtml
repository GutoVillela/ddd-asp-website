<!-- Pagination -->
<nav aria-label="Brands page navigation">
    <ul class="pagination justify-content-end">
        <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : string.Empty)">
            <a class="page-link" asp-controller="@ViewData["Controller"]" asp-controller="@ViewData["Action"]" asp-route-page="@(Model.CurrentPage - 1)"  asp-route-queryByName="@ViewData["QueryByName"]">Anterior</a>
        </li>
        @for (int i = Model.StartPage; i <= Model.EndPage; i++)
        {

            <li class="page-item @(Model.CurrentPage == i ? "active" : string.Empty)">
                <a class="page-link" asp-controller="@ViewData["Controller"]" asp-controller="@ViewData["Action"]" asp-route-page="@i" asp-route-queryByName="@ViewData["QueryByName"]">@i</a>
            </li>
        }
        <li class="page-item @(Model.CurrentPage == Model.TotalPages || Model.TotalPages == 0 ? "disabled" : string.Empty)">
            <a class="page-link" asp-controller="@ViewData["Controller"]" asp-controller="@ViewData["Action"]" asp-route-page="@(Model.CurrentPage + 1)"  asp-route-queryByName="@ViewData["QueryByName"]">Próximo</a>
        </li>
    </ul>
</nav>
<!-- END Pagination -->